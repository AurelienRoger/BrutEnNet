import './Footer.scss';

function Footer() {
  return (
    <div className="footer">
      <a href="https://github.com/AurelienRoger/BrutEnNet/issues" target="_blank" rel="noreferrer">Une suggestion ? Un bug ? Par ici</a>
    </div>
  );
}

export default Footer;
